// Monorepo root App.tsx — loaded by b3hub/node_modules/expo/AppEntry.js
// when expo is hoisted. The Metro redirect in apps/mobile/metro.config.js
// normally intercepts the "../../App" import and sends it to
// apps/mobile/App.tsx instead, but this file is kept as a fallback.
export { default } from './apps/mobile/App';
