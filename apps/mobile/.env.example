# Copy to .env.local and fill in real values
# Get a key from https://console.cloud.google.com
#
# Required APIs to enable on your key:
#   - Maps SDK for iOS
#   - Maps SDK for Android
#   - Places API           (AddressPicker autocomplete)
#   - Geocoding API         (reverse geocode drag pin → address)
#   - Routes API v2         (real road polylines + distance/ETA in JobRouteMap)
#   - Route Optimization API (multi-stop tour planning for drivers)
#   - Navigation SDK        (in-app turn-by-turn — needs custom dev build)

EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=
EXPO_PUBLIC_GOOGLE_MAPS_KEY=
EXPO_PUBLIC_API_URL=http://localhost:3000/api/v1
