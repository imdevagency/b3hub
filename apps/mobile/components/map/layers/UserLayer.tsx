/**
 * UserLayer — kept as a no-op stub so existing imports don't break.
 *
 * With react-native-maps, user location is shown via the
 * `showsUserLocation` prop on BaseMap. Pass <BaseMap showsUserLocation> instead.
 */
export function UserLayer(_props?: { visible?: boolean; renderMode?: unknown }) {
  return null;
}
