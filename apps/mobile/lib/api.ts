/**
 * lib/api.ts — Barrel re-export
 *
 * All domain-specific logic lives in lib/api/*.ts.
 * This file keeps every existing import (`from '@/lib/api'`) working unchanged.
 */
export * from './api/index';
