node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# Compiled seed artifacts — commit prisma/*.ts source only, not JS output
prisma/*.js
prisma/*.js.map
prisma/*.d.ts
