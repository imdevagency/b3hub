# Environment variables
.env
.env.local
.env.*.local

# Server-only key for Google Routes API calls from backend.
# Restrict this key by API (Routes API only) and by source IP/service account where possible.
GOOGLE_MAPS_SERVER_API_KEY=
