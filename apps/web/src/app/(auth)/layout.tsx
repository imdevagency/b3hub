/**
 * Auth section layout.
 * Shared wrapper around login and register pages (centers the form card).
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
